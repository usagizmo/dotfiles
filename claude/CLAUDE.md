# CLAUDE.md

## プラン作成・実装方針

- プランはフェーズ分けせず「最終形」を 1 本のプランとして提示し、ユーザー確認後にすべて実装してから動作確認する
- 最終形に不要なコード（feature flag / deprecated alias / 互換 shim / 後で削除する前提の温存コード）は初手から書かない
- 1 セッションで複数の独立した課題に対応するときは、課題ごとに `/commit` を分ける
- 実装完了後は `/tidy` → `/docs` → `/commit` の順で実行する
