# herdr が作る worktree 内の repo 設定を一括で信頼する
[settings]
trusted_config_paths = ["~/.herdr/worktrees"]

[tools]
node = "24"
# JS/TS 系の lint / format。commit gate（.githooks/pre-commit）とエディタの両方がこれを使う
oxfmt = "latest"
oxlint = "latest"
uv = "latest"
