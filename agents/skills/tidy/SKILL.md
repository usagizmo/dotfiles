---
name: tidy
description: コミット前のクリーンアップ。後方互換のためだけの古い記述・dead code を検出し、クリーンな実装を提案する。ユーザーが /tidy と言ったとき、または codex-consult / codex-review-loop を使った機能実装のコミット前に使う（すべてのコミットで回すと重いため、軽微な変更では不要）。
---

# コードクリーンアップチェック

実装者のバイアスを排除するため、サブエージェントが使える環境では第三者視点でレビューさせ、使えない環境では同じ観点で自己レビューする。

## 実行手順

1. 今回のセッションで変更したファイルパスを把握する
2. サブエージェントが使える環境では、変更ファイルパスを含めて以下のプロンプトを渡す。使えない環境では、このプロンプトを自己レビューのチェックリストとして使う:

```
コードクリーンアップの観点からレビューしてください。
実装の経緯は考慮せず、今のコードだけを見て判断してください。

## 変更ファイル
{変更したファイルパスの一覧}

## 変更内容の把握方法
`git diff --cached` と `git diff` を実行してください。
diff が空の場合は `git log -1 --format="" --name-only` で直近コミットの変更ファイルを確認し、`git diff HEAD~1` で差分を取得してください。

## レビュー観点

### 設計の妥当性
> トップエンジニアが今日、一から設計したとしても同じ設計にするか？
「いいえ」なら、クリーンな設計を提案する。

### ボーイスカウトルール
変更ファイルとその周辺で `~/.agents/rules/boy-scout.md` に記載の改善を確認・適用する。
今回の変更に関係ない箇所でも、目に入った改善は積極的に行う。

### 周辺との整合性
変更内容が周辺のコード・設定と矛盾していないか確認する。
同じパターンやルールを使っている兄弟ファイルがあれば、そちらにも同じ変更が必要でないか確認する。

### 追加の確認観点
- `TODO: remove after migration` 等のコメントが付いた暫定コード
- 後方互換のためだけの型エイリアス・ラッパー・re-export・`@deprecated`
- 使われていないフィーチャーフラグ
- 過去の制約に基づいた回避策や workaround

## 出力形式
- 修正すべき箇所: ファイルパス・行番号・修正内容を具体的に
- 問題なしの場合: 「問題なし」とだけ回答
```

3. レビュー結果に基づき、修正を適用する
4. 修正した箇所を簡潔に報告する
