---
name: docs
description: >-
  仕様変更・機能実装を文書に反映するために実行する。中規模以上の仕上げでコミット前に
  project docs / agent-facing 文書（AGENTS / skills / prompts / references）の更新要否を判定し更新する。
  agent-facing 文書を書いた・編集した変更は、規模に関わらずコミット前に品質パスを実行する。
  製品コード実装そのものには使わない。
---

# ドキュメント

## いつ

| トリガー | やること |
| --- | --- |
| 仕様変更・機能実装があった（中規模以上の仕上げ） | コミット前に project docs / agent-facing 文書の更新要否を判定し更新する。agent-facing を更新したら品質パスも通す |
| AGENTS / skills / prompts / references を新規・編集した | コミット前に品質パスを実行する。規模不問。下書きのままで commit しない |
| 使用した AGENTS / skills の不備（誤誘導・欠落・冗長・手順の摩擦）に気づいた | 自明な修正はその場で直して品質パスを通す。判断が要るものはユーザーに提案する |

規模 SSOT は `~/.agents/AGENTS.md`。available skills に `docs-project` があれば先に invoke し、本 skill への project 差分（追加・具体化のみ。基準・完了条件を緩めない）として適用する。project 側は `docs-project` 固定名で置き、単体では invoke しない。

## 契約

- 下書きは判断材料の抜け漏れを優先してよい。品質は品質パスが担保する
- 該当トリガーをすべて完了して docs 完了。未完了なら commit しない
- レビュー中は変更しない。適用・報告は親。subagent が使えなければ同じ観点で自己レビューする
- harness 固有の agent API は指定しない
- global（`~/.agents/` = dotfiles agents）の更新が必要と判断したら更新してよい

## 品質パス（基準 + 手順）

agent-facing 文書の品質 SSOT。global も project も同じ。

基準:

- **抜け漏れなく、要点だけ**: 判断に必要な不変条件・境界・例外は落とさない。言い換えの重複・経緯・メタ説明は書かない
- **AGENTS / skills は薄く**: 発動条件・判断・恣意的な規約（命名・境界・優先順位の決定）・SSOT への誘導に留め、モデルが自力で再導出できる一般ベストプラクティスは書かない。方向性を定める文（基準の高さ・どちらに倒すか）は残し、挙動を変えない飾り文だけ削る。状況限定の詳細・事例は `references/` へ。数行で書ける知見は reference を新設せず本文で圧縮する
- **行動を変えられる具体性**: 何をするか / 何をしないか / SSOT はどこかを明示する。装飾・自己言及は削る
- **非自明な境界を持つ API は動く正例を残す**: prose の言い換えで代替しない。入口では詳細カタログを複製せず深い SSOT へ誘導する
- **repo 内で完結させる**: Issue / PR 番号等の外部リンクを張らない。仕様・判断は文書本文に書き、参照は repo 内へ張る

手順:

1. 節単位で書き直して基準へ適合させる。太字剥がし・言い換え削除だけの diff は不合格
2. 大型 reference（約 300 行超）は見出し一覧・表 / fence を再読する第 2 パスを必須にする
3. プロジェクトに Markdown lint があれば通す

**完了条件**: 必要な判断情報の被覆を維持または改善し、より短く明確になっていること。行数だけが減った変更や構造が壊れた変更は未完了。

## project docs 更新判定

確立済みの知見の永続化判定と、触った docs 周辺の削除・最新化。新しい設計原則の発見器ではない。追加の前に削れる箇所を先に洗う。起点外でも削れる・直せる記述は直し、テーマが完全に別の大規模再構成だけ別提案にする。

1. `git status --short` で起点を確定する（混在 working tree は絞る。起点と diff の食い違いは報告）
2. 材料を整理する: **知見**は検証可能な罠・正解パターン。**設計上の選択**は「A ではなく B」の主張で照合対象。単独で一般原則にしない
3. `references/review-prompt.md` に起点・知見・選択を埋めて渡す。使えなければ自己レビュー
4. 妥当な更新は起点外でも適用する。agent-facing を更新したら同じコミット前に品質パスも通す
5. 更新・見送りを簡潔に報告する

**完了条件**: 起点に対する更新要否を判定し、要なら適用済み、不要なら見送り理由が言えること。
